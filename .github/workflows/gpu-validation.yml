name: GPU validation

on:
  workflow_dispatch:
    inputs:
      run-timed-benchmarks:
        description: "Run Criterion timing benches on the GPU runners"
        type: boolean
        required: false
        default: false
      run-metal-validation:
        description: "Run Apple Silicon Metal validation jobs"
        type: boolean
        required: false
        # Default off: with no Metal runner online the job sits queued forever,
        # which holds the whole run "in progress" and locks every job's logs
        # (including the CUDA gate). Opt in explicitly when a Metal runner is up.
        default: false
      run-linux-ci:
        description: "Run the Linux CI gate on the self-hosted CUDA runner"
        type: boolean
        required: false
        default: false
      run-nvidia-baseline:
        description: "Run the nvJPEG/nvJPEG2000 baseline comparison (requires nvJPEG2000 installed on the runner)"
        type: boolean
        required: false
        default: false
      run-cuda-htj2k-decode-profile:
        description: "Collect CUDA HTJ2K decode RCA/profile log, trace, and Criterion artifacts"
        type: boolean
        required: false
        default: false

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  linux-ci:
    if: ${{ inputs.run-linux-ci }}
    name: Linux CI on self-hosted CUDA runner
    runs-on: [self-hosted, Linux, X64, cuda]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Show runner diagnostics
        run: |
          uname -a
          rustc -Vv
          cargo -V
      - run: cargo xtask ci
      - run: cargo xtask no-std
      - run: cargo xtask bench-build
      - run: cargo xtask fuzz-build
      - name: Install CI helper tools
        run: |
          cargo install typos-cli --version 1.42.3 --locked
          cargo install cargo-deny --locked
          cargo install cargo-llvm-cov --locked
      - run: cargo xtask typos
      - run: cargo xtask deny
      - run: cargo xtask coverage

  metal-apple-silicon:
    if: ${{ inputs.run-metal-validation }}
    name: Metal validation on Apple Silicon
    runs-on: [self-hosted, macOS, ARM64, metal]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test -p signinum-jpeg-metal --all-targets
      - run: cargo test -p signinum-j2k-metal --all-targets
      - run: cargo bench -p signinum-jpeg-metal --no-run
      - run: cargo bench -p signinum-j2k-metal --no-run
      - run: cargo bench -p signinum-j2k-metal --bench encode_stages --no-run
      - run: cargo bench -p signinum --bench facade --features metal --no-run
      - if: ${{ inputs.run-timed-benchmarks }}
        env:
          SIGNINUM_GPU_BENCH_DIM: "1024"
          SIGNINUM_GPU_BENCH_BATCH: "64"
          SIGNINUM_GPU_BENCH_BATCH_DIM: "1024"
        run: cargo bench -p signinum-jpeg-metal --bench device_upload -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2
      - if: ${{ inputs.run-timed-benchmarks }}
        run: cargo bench -p signinum-jpeg-metal --bench compare -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2
      - if: ${{ inputs.run-timed-benchmarks }}
        run: cargo bench -p signinum-j2k-metal --bench compare -- --sample-size 10 --warm-up-time 1 --measurement-time 2
      - if: ${{ inputs.run-timed-benchmarks }}
        env:
          SIGNINUM_REQUIRE_METAL_BENCH: "1"
        run: cargo bench -p signinum-j2k-metal --bench encode_stages -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2
      - if: ${{ inputs.run-timed-benchmarks }}
        run: cargo bench -p signinum --bench facade --features metal -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2

  # cuda-x86_64-compatibility is THE authoritative CUDA parity gate.
  # It must be run (via workflow_dispatch) before merging any change to the
  # CUDA encode path (crates/signinum-j2k-cuda, crates/signinum-cuda-runtime).
  # The job is workflow_dispatch-only; do not add push/pull_request triggers
  # without a policy decision — these are cost-sensitive self-hosted GPU runners.
  cuda-x86_64-compatibility:
    name: CUDA API compatibility on x86_64
    runs-on: [self-hosted, Linux, X64, cuda]
    env:
      SIGNINUM_REQUIRE_CUDA_RUNTIME: "1"
      SIGNINUM_REQUIRE_CUDA_HTJ2K_STRICT: "1"
      SIGNINUM_REQUIRE_CUDA_JPEG_HARDWARE_DECODE: "1"
      RUST_TEST_THREADS: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Assert CUDA runtime gate is set
        run: |
          test -n "${SIGNINUM_REQUIRE_CUDA_RUNTIME:-}" || { echo "SIGNINUM_REQUIRE_CUDA_RUNTIME not set — parity tests would silently skip"; exit 1; }
      - name: Show CUDA runner diagnostics
        run: |
          uname -a
          rustc -Vv
          cargo -V
          if command -v nvidia-smi >/dev/null 2>&1; then
            nvidia-smi
          else
            echo "nvidia-smi not found; CUDA runtime validation requires a working CUDA driver"
          fi
          command -v nvcc
          nvcc --version
          if command -v ldconfig >/dev/null 2>&1; then
            ldconfig -p | grep -i nvjpeg || true
          fi
      - run: cargo test -p signinum-cuda-runtime --all-targets
      - run: cargo test -p signinum-cuda-runtime --all-targets --features cuda-profiling
      - run: cargo test -p signinum-jpeg-cuda --all-targets --features cuda-runtime
      - run: cargo test -p signinum-j2k-cuda --all-targets --features cuda-runtime
      - name: Run HTJ2K encode parity tests with executed-count floor
        run: |
          out=$(cargo test -p signinum-j2k-cuda --features cuda-runtime --test htj2k_encode_parity -- --nocapture 2>&1)
          echo "$out"
          passed=$(echo "$out" | sed -n 's/^test result: ok\. \([0-9]\+\) passed.*/\1/p' | head -n1)
          echo "parity tests passed: ${passed:-0}"
          test "${passed:-0}" -ge 8 || { echo "Expected at least 8 executed parity tests, got ${passed:-0} — tests may have silently skipped"; exit 1; }
      - run: cargo test -p signinum-transcode-cuda --all-targets --features cuda-runtime
      - name: Run JPEG->HTJ2K transcode parity tests with executed-count floor
        run: |
          out=$(cargo test -p signinum-transcode-cuda --features cuda-runtime --test reversible_dwt53_parity --test dwt97_parity --test dwt97_batch_parity --test htj2k97_codeblock_parity --test jpeg_to_htj2k -- --nocapture 2>&1)
          echo "$out"
          passed=$(echo "$out" | sed -n 's/^test result: ok\. \([0-9]\+\) passed.*/\1/p' | awk '{s+=$1} END{print s+0}')
          echo "transcode parity tests passed: ${passed:-0}"
          test "${passed:-0}" -ge 7 || { echo "Expected at least 7 executed transcode parity tests, got ${passed:-0} — tests may have silently skipped"; exit 1; }
      - run: cargo test -p signinum-j2k-cuda --all-targets --features cuda-profiling
      # Opt-in nvJPEG/nvJPEG2000 baseline. Requires a separately-installed
      # libnvjpeg2k (set NVJPEG2K_LIB_DIR / NVJPEG2K_INCLUDE_DIR if it is not under
      # /usr/local/cuda). Set the repo variable SIGNINUM_BENCH_SVS to an .svs on
      # the runner to extract real WSI tiles; or SIGNINUM_BENCH_JPEG_DIR to a
      # pre-extracted tile directory. With neither, it runs the committed pancreas
      # tile corpus and requires at least 100 benchmark tiles.
      - name: NVIDIA baseline comparison (nvJPEG + nvJPEG2000)
        if: ${{ inputs.run-nvidia-baseline }}
        env:
          SIGNINUM_REQUIRE_CUDA_RUNTIME: "1"
          SIGNINUM_REQUIRE_CUDA_HTJ2K_STRICT: "1"
          SIGNINUM_REQUIRE_NV_BASELINE_BUILD: "1"
          SIGNINUM_BENCH_SVS: ${{ vars.SIGNINUM_BENCH_SVS }}
          SIGNINUM_BENCH_JPEG_DIR: ${{ vars.SIGNINUM_BENCH_JPEG_DIR }}
        run: |
          set -e
          DIR="${SIGNINUM_BENCH_JPEG_DIR:-}"
          if [ -z "$DIR" ] && [ -n "${SIGNINUM_BENCH_SVS:-}" ]; then
            if [ ! -f "${SIGNINUM_BENCH_SVS}" ]; then
              echo "Configured SIGNINUM_BENCH_SVS does not exist: ${SIGNINUM_BENCH_SVS}" >&2
              exit 1
            fi
            echo "extracting benchmark tiles from ${SIGNINUM_BENCH_SVS}"
            cargo run --release -p signinum-nvidia-baseline --bin svs_extract -- \
              "${SIGNINUM_BENCH_SVS}" "${RUNNER_TEMP:-/tmp}/svs_bench_tiles" \
              --limit 256 --stride 51 --quality 85 --min-tissue 0.6
            DIR="${RUNNER_TEMP:-/tmp}/svs_bench_tiles"
          fi
          # Default to the committed pancreas tissue tiles when nothing else is set.
          if [ -z "$DIR" ]; then
            DIR="crates/signinum-nvidia-baseline/benchtiles/pancreas"
          fi
          export SIGNINUM_BENCH_JPEG_DIR="$DIR"
          echo "benchmark JPEG dir: ${SIGNINUM_BENCH_JPEG_DIR}"
          cargo test -p signinum-nvidia-baseline --features nvjpeg2000 --all-targets
          cargo clippy -p signinum-nvidia-baseline --features nvjpeg2000 --all-targets -- -D warnings
          cargo run --release -p signinum-nvidia-baseline --features nvjpeg2000 --bin transcode_compare -- \
            --quant-scales 1.55,1.70,1.85,1.90,2.05,2.25 \
            --match-nvidia-bytes \
            --match-tolerance 0.02 \
            --min-tiles 100 \
            --json target/transcode_compare.json \
            --csv target/transcode_compare.csv
          python3 -m json.tool target/transcode_compare.json >/dev/null
          cargo run --release -p signinum-nvidia-baseline --features nvjpeg2000 --bin decode_compare -- \
            --jpeg-dir "${SIGNINUM_BENCH_JPEG_DIR}" \
            --warmup 2 \
            --iterations 10 \
            --min-inputs 2 \
            --json target/decode_compare.json \
            --csv target/decode_compare.csv
          python3 -m json.tool target/decode_compare.json >/dev/null
      - name: Upload NVIDIA baseline comparison artifacts
        if: ${{ inputs.run-nvidia-baseline && always() }}
        uses: actions/upload-artifact@v4
        with:
          name: nvidia-baseline-comparison
          path: |
            target/transcode_compare.json
            target/transcode_compare.csv
            target/decode_compare.json
            target/decode_compare.csv
          if-no-files-found: warn
      - run: cargo clippy -p signinum-cuda-runtime --all-targets -- -D warnings
      - run: cargo clippy -p signinum-cuda-runtime --all-targets --features cuda-profiling -- -D warnings
      - run: cargo clippy -p signinum-j2k-cuda --all-targets --features cuda-runtime -- -D warnings
      - run: cargo clippy -p signinum-transcode-cuda --all-targets --features cuda-runtime -- -D warnings
      - run: cargo clippy -p signinum-j2k-cuda --all-targets --features cuda-profiling -- -D warnings
      - run: cargo bench -p signinum-jpeg-cuda --bench device_decode --features cuda-runtime --no-run
      - run: cargo bench -p signinum-j2k-cuda --bench encode_stages --features cuda-runtime --no-run
      - run: cargo bench -p signinum-j2k-cuda --bench htj2k_decode --features cuda-runtime --no-run
      - run: cargo bench -p signinum-j2k-cuda --bench htj2k_encode --features cuda-runtime --no-run
      - run: cargo bench -p signinum --bench facade --features cuda-runtime --no-run
      - if: ${{ inputs.run-timed-benchmarks }}
        env:
          SIGNINUM_REQUIRE_CUDA_BENCH: "1"
          SIGNINUM_GPU_BENCH_DIM: "4096"
          SIGNINUM_GPU_BENCH_BATCH: "64"
          SIGNINUM_GPU_BENCH_BATCH_DIM: "1024"
        run: cargo bench -p signinum-jpeg-cuda --bench device_decode --features cuda-runtime -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2
      - if: ${{ inputs.run-timed-benchmarks }}
        env:
          SIGNINUM_REQUIRE_CUDA_BENCH: "1"
        run: cargo bench -p signinum-j2k-cuda --bench encode_stages --features cuda-runtime -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2
      - if: ${{ inputs.run-timed-benchmarks }}
        env:
          SIGNINUM_REQUIRE_CUDA_BENCH: "1"
        run: cargo bench -p signinum-j2k-cuda --bench htj2k_decode --features cuda-runtime -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2
      - name: CUDA HTJ2K decode RCA profile
        if: ${{ inputs.run-cuda-htj2k-decode-profile }}
        env:
          SIGNINUM_REQUIRE_CUDA_RUNTIME: "1"
          SIGNINUM_REQUIRE_CUDA_HTJ2K_STRICT: "1"
          SIGNINUM_REQUIRE_CUDA_BENCH: "1"
          SIGNINUM_J2K_PROFILE_STAGES: summary
          SIGNINUM_J2K_CUDA_TRACE: target/cuda_htj2k_decode_trace.json
        run: |
          set -o pipefail
          mkdir -p target
          if ! command -v samply >/dev/null 2>&1; then
            RUSTFLAGS="" cargo install samply --version 0.13.1 --locked
          fi
          samply --version
          samply record --save-only -o target/cuda_htj2k_decode_samply.json.gz -- \
            cargo bench -p signinum-j2k-cuda --bench htj2k_decode --features cuda-runtime,cuda-profiling -- \
            --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2 \
            2>&1 | tee target/cuda_htj2k_decode_profile.log
      - name: Upload CUDA HTJ2K decode RCA profile artifacts
        if: ${{ inputs.run-cuda-htj2k-decode-profile && always() }}
        uses: actions/upload-artifact@v4
        with:
          name: cuda-htj2k-decode-rca-profile
          path: |
            target/cuda_htj2k_decode_profile.log
            target/cuda_htj2k_decode_trace.json
            target/cuda_htj2k_decode_samply.json.gz
            target/criterion
          if-no-files-found: warn
      - if: ${{ inputs.run-timed-benchmarks }}
        env:
          SIGNINUM_REQUIRE_CUDA_BENCH: "1"
        run: cargo bench -p signinum-j2k-cuda --bench htj2k_encode --features cuda-runtime -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2
      - if: ${{ inputs.run-timed-benchmarks }}
        env:
          SIGNINUM_REQUIRE_CUDA_BENCH: "1"
        run: cargo bench -p signinum --bench facade --features cuda-runtime -- --noplot --sample-size 10 --warm-up-time 1 --measurement-time 2
