# Corpus

This directory stores small committed conformance fixtures and metadata for
larger external corpora used during validation.

Committed fixtures are intentionally limited. Larger WSI, JPEG, J2K, and HTJ2K
inputs must be supplied through environment variables or external corpus
locations and must not be committed accidentally.

Fixture inventory should be derived from manifests and directory contents, not
from stale prose.
