# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.80"
name = "dicom-toolkit-jpeg2000"
version = "0.5.0"
authors = ["Laurenz Stampfl <laurenz.stampfl@gmail.com>"]
build = false
exclude = [
    "/tests/",
    "/manifest_*.json",
    "/sync.py",
    "/TESTS.md",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A memory-safe, pure-Rust JPEG 2000 codec with native-bit-depth decode and DICOM-focused encoding support."
homepage = "https://github.com/knopkem/dicom-toolkit-rs"
documentation = "https://docs.rs/dicom-toolkit-jpeg2000"
readme = "README.md"
keywords = [
    "jpeg2000",
    "j2k",
    "dicom",
    "image",
    "codec",
]
categories = [
    "multimedia::images",
    "encoding",
    "science",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/knopkem/dicom-toolkit-rs"

[features]
default = [
    "std",
    "openjph-htj2k",
]
logging = ["dep:log"]
openjph-htj2k = [
    "std",
    "dep:openjph-core",
]
simd = []
std = []

[lib]
name = "dicom_toolkit_jpeg2000"
path = "src/lib.rs"

[dependencies.log]
version = "0.4"
optional = true

[dependencies.openjph-core]
path = "../openjph-core"
optional = true
