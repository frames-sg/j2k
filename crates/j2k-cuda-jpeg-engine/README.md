# j2k-cuda-jpeg-engine

CUDA baseline JPEG kernel ownership, validation, and orchestration used by the
public JPEG CUDA adapter.

This crate is a low-level implementation dependency. Most applications should
use `j2k-jpeg-cuda`.
