# j2k-cuda-transcode-engine

CUDA coefficient-domain transform and quantization engine used by the public
J2K transcode adapter.

This crate is a low-level implementation dependency. Most applications should
use `j2k-transcode-cuda`.
