# j2k-cuda-build-support

Shared build-time support for staging CUDA-Oxide kernel projects and packaging
their PTX images for the J2K CUDA engine crates.

This crate is an implementation dependency. Applications should use the public
codec adapters instead of calling it directly.
