# signinum-transcode-cuda

CUDA acceleration adapter for Signinum JPEG to HTJ2K transcode stages.

This crate accelerates supported transform and code-block preparation stages.
It does not replace the transcode API in `signinum-transcode`.
