# J2K-M3 HT Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** make `slidecodec-j2k` reliably inspect and decode HTJ2K codestreams and JP2 files through the in-tree decoder path.

**Architecture:** keep the committed M2 surface, add HT-aware marker handling in the decoder, and lock the behavior with `encode_htj2k`-generated regression tests across full-frame and WSI API surfaces.

**Tech Stack:** Rust, `slidecodec-j2k`, `slidecodec-core`

---

### Task 1: Add failing HT coverage

**Files:**
- Modify: `crates/slidecodec-j2k/tests/decode.rs`
- Modify: `crates/slidecodec-j2k/tests/inspect.rs`

- [ ] **Step 1: Add failing tests using `encode_htj2k`**
- [ ] **Step 2: Run the targeted J2K tests to verify the gap**
Run: `cargo test -p slidecodec-j2k`

### Task 2: Make inspect and decode paths HT-safe

**Files:**
- Modify: `crates/slidecodec-j2k/src/view.rs`
- Modify: `crates/slidecodec-j2k/src/lib.rs` if new exports are needed

- [ ] **Step 1: Make `J2kDecoder::inspect` use the same HT-aware parser path as `J2kView::parse`**
- [ ] **Step 2: Confirm the decode surfaces exercise the crate's own HT path, not a backend shortcut**
- [ ] **Step 3: Re-run targeted tests**
Run: `cargo test -p slidecodec-j2k`

### Task 3: Milestone verification and commit

- [ ] **Step 1: Run milestone verification**
Run:
- `cargo test --workspace`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo check --manifest-path crates/slidecodec-j2k/fuzz/Cargo.toml`
- [ ] **Step 2: Commit**
Commit message: `feat: add htj2k decode coverage`
